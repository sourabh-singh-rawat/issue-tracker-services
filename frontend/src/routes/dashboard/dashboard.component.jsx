import { AppContainer, PageTitle } from "../../App.styles";

const Dashboard = () => {
    return (
        <AppContainer>
            <PageTitle>DASHBOARD</PageTitle>
        </AppContainer>
    );
};

export default Dashboard;
