import { AppContainer, PageTitle } from "../../App.styles";

const Teams = () => {
    return (
        <AppContainer>
            <PageTitle>TEAMS</PageTitle>
        </AppContainer>
    );
};

export default Teams;
