services:
  identity-postgres:
    image: postgres:18.4
    restart: unless-stopped
    environment:
      - POSTGRES_USER=identity
      - POSTGRES_PASSWORD=${POSTGRES_IDENTITY_PASSWORD}
      - POSTGRES_DB=identity
    volumes:
      - ../data/docker/pg-identity-data:/var/lib/postgresql
    command:
      - postgres
      - -c
      - max_connections=50
    ports:
      - 5433:5432
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M

  issues-postgres:
    image: postgres:18.4
    restart: unless-stopped
    environment:
      - POSTGRES_USER=issues
      - POSTGRES_PASSWORD=${POSTGRES_ISSUES_PASSWORD}
      - POSTGRES_DB=issues
    volumes:
      - ../data/docker/pg-issues-data:/var/lib/postgresql
    command:
      - postgres
      - -c
      - max_connections=50
    ports:
      - 5434:5432
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M

  inventory-postgres:
    image: postgres:18.4
    restart: unless-stopped
    environment:
      - POSTGRES_USER=inventory
      - POSTGRES_PASSWORD=${POSTGRES_INVENTORY_PASSWORD}
      - POSTGRES_DB=inventory
    volumes:
      - ../data/docker/pg-inventory-data:/var/lib/postgresql
    command:
      - postgres
      - -c
      - max_connections=50
    ports:
      - 5437:5432
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M

  attachment-postgres:
    image: postgres:18.4
    restart: unless-stopped
    environment:
      - POSTGRES_USER=attachment
      - POSTGRES_PASSWORD=${POSTGRES_ATTACHMENT_PASSWORD}
      - POSTGRES_DB=attachment
    volumes:
      - ../data/docker/pg-attachment-data:/var/lib/postgresql
    command:
      - postgres
      - -c
      - max_connections=50
    ports:
      - 5436:5432
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M

  notification-postgres:
    image: postgres:18.4
    restart: unless-stopped
    environment:
      - POSTGRES_USER=notification
      - POSTGRES_PASSWORD=${POSTGRES_NOTIFICATION_PASSWORD}
      - POSTGRES_DB=notification
    volumes:
      - ../data/docker/pg-notification-data:/var/lib/postgresql
    command:
      - postgres
      - -c
      - max_connections=50
    ports:
      - 5435:5432
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 128M

  # Register per-service Postgres servers (plus Ory) in pgAdmin
  pgadmin:
    environment:
      - PGADMIN_CONFIG_TYPE=multi-db
