# Deployment configuration
replicaCount: 1

# Pod configuration
container:
  image: sourabhrawatcc/email
  exposedPort: 4000

# Service configuration

# Jwt secret
jwtSecretRef: jwt-secret
smtpSecretRef: smtp-secret