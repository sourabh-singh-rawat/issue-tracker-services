type Attachment {
  bucket: String!
  id: String!
  thumbnailLink: String!
}

type Mutation {
  deleteAttachment(id: String!): String!
}

type PaginatedAttachment {
  rowCount: Float!
  rows: [Attachment!]!
}

type Query {
  findAttachments(issueId: String!): PaginatedAttachment!
}
