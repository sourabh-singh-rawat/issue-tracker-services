apiVersion: skaffold/v4beta6
kind: Config
metadata:
  name: issue-tracker
build:
  artifacts:
    - image: sourabhrawatcc/identity
      context: identity
      docker:
        dockerfile: Dockerfile
manifests:
  rawYaml:
    - k8s/identity/identity.depl.yaml
    - k8s/identity/identity-pg.depl.yaml
